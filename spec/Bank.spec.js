const Bank = require("../src/Bank");

describe("Bank", () => {
    let bank;
  
    beforeEach(() => {
      bank = new Bank();
    });
  
    it("", () => {
      //set up
      const expected = "";
      //execute
      const result = ;
      //verify
      expect(expected).toEqual(result);
    });
  
})